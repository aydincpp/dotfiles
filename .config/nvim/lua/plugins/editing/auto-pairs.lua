return {
  "windwp/nvim-autopairs",

  -- Automatically load and setup the plugin with default options
  config = true
  -- Alternative: use opts = {} to pass custom setup options
  -- This is equivalent to calling require("nvim-autopairs").setup({})
}
